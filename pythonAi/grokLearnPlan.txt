第一阶段（第1-20天）：核心产品交付（智能研究助手 V1）目标：命令行 + Web 版 Agent（LangGraph ReAct + 多工具 + 完整 RAG），可面试 Demo。
第1-7天：基础 + RAG 链Day 1：环境搭建 + LangChain 入门（已完成）。
Day 2：工具调用基础（计算器 + @tool
）。
Day 3：多工具 + 记忆（天气 + 时间 + 手动 ReAct）。
Day 4：调试优化 + 思考链可见。
Day 5：升级 LangGraph ReAct Agent + 初步 RAG 工具（已完成）。
Day 6：完整 RAG 链（retriever + prompt + LLM + MMR + 多查询）。
Day 7：RAG 测试 + 优化（多轮追问 + token 管理 + compression + HyDE 预热）。

第8-15天：RAG 进阶 + PromptDay  
Day8：RAG链高级模式（map_reduce/refine + stuff，5小时）【优化：融入知识库调整】
子任务：实现三种链类型；用知识库测试性能；集成到smart_document_qa工具。
步骤：准备知识库文件（rules.md, star.md, template1.md）；加载到Chroma（DocumentLoader + 嵌入）；用map_reduce总结规则（e.g., “总结简历写作技巧”）；refine迭代润色用户简历（结合“袁麟翔简历”+规则）；比较token用量/准确（测试10查询，如“用量化优化经历”）。
参考：https://python.langchain.com/v0.2/docs/tutorials/rag/#advanced-rag-techniques；https://python.langchain.com/v0.2/docs/integrations/document_loaders/unstructured/（加载MD）。
产出：AdvancedRAG.py + knowledge_base/文件夹，commit “Day8: Advanced RAG chains with resume rules knowledge base”。

Day9：混合检索 + 重排序 + metadata过滤（5小时）【优化：知识库检索优化】
子任务：实现EnsembleRetriever（语义+关键词）；加rerank（Cohere或自定义）；metadata过滤（e.g., type: 'rule' or 'template'）。
步骤：为知识库加metadata（e.g., {'source': 'star.md', 'type': 'law'}）；测试混合检索（如“STAR + 我的经历” → 过滤召回规则+“袁麟翔简历”片段）；评估召回提升。
参考：https://python.langchain.com/v0.2/docs/how_to/ensemble_retriever/；https://python.langchain.com/v0.2/docs/how_to/add_metadata/。
产出：更新RAG_Pipeline.py，commit “Day9: Hybrid retrieval with rules filtering”。

Day10：Prompt工程精通（few-shot + CoT + 系统提示优化ReAct，5小时）【优化：润色prompt用规则】
子任务：自定义prompt模板；few-shot示例从知识库抽取（e.g., “输入:原经历 输出:用STAR+量化的优版”）；CoT多步（先查规则，再应用）。
步骤：优化ReAct prompt强调“用本地知识库规则润色”；测试多轮（如先问技巧，再润色“袁麟翔简历”）。
参考：https://python.langchain.com/v0.2/docs/how_to/few_shot_examples/；https://python.langchain.com/v0.2/docs/how_to/chain_of_thought/。
产出：prompts.py（含规则few-shot），commit “Day10: Prompt engineering for resume polishing with rules”。

Day11：数据清洗实战（pandas清洗PDF/CSV + 集成工具，5小时）【新增：清洗知识库+模板】
子任务：用PyMuPDF/Pandas解析/清洗规则/模板文件（e.g., 提取关键词列表）；建clean_resume工具（清洗用户上传+合并规则）。
步骤：清洗“袁麟翔简历”PDF（去噪、段落提取）；清洗rules.md（pandas DataFrame存技巧）；集成到RAG预处理。
参考：https://python.langchain.com/v0.2/docs/integrations/document_loaders/pymupdf/；pandas docs。
产出：data_cleaning.py，commit “Day11: Cleaning for resume rules/templates and user files”。

Day12：向量检索进阶（多查询扩展 + 评估预热，5小时）【优化：评估规则召回】
子任务：MultiQueryRetriever生成变体查询（e.g., “简历量化技巧 synonyms”）；用Evaluator评估RAG（召回率/准确，针对规则应用）。
步骤：测试集含规则查询（e.g., 10个“润色+STAR”）；计算指标（LangChain PairwiseEvaluator）。
参考：https://python.langchain.com/v0.2/docs/how_to/multi_query/；https://python.langchain.com/v0.2/docs/guides/evaluation/。
产出：evaluation.py，commit “Day12: Advanced vector search with rules eval”。

Day13：NLP/ML基础（Tokenization + PyTorch张量 + 简单分类，5小时）【优化：分类简历/规则段落】
子任务：HuggingFace tokenizer分词规则文本；PyTorch模型分类（e.g., 规则 vs 模板 vs 用户经历）。
步骤：Notebook用“袁麟翔简历”+rules.md训练简单分类器；集成到清洗工具。
参考：https://huggingface.co/docs/transformers/tokenizer；https://pytorch.org/tutorials/beginner/basics/quickstart_tutorial.html。
产出：nlp_basics.ipynb，commit “Day13: NLP/ML basics for resume sections”。

Day14：微调概念（LoRA + PEFT示例Notebook，5小时）【优化：SFT预热用规则数据】
子任务：LoRA/PEFT理论；HuggingFace datasets加载规则+模板数据集（e.g., 原描述-优描述对）。
步骤：准备小数据集（从rules.md生成指令对，如“应用STAR: 输入X 输出Y”）。
参考：https://huggingface.co/docs/peft/en/conceptual_guides/lora；https://huggingface.co/docs/datasets/。
产出：finetune_intro.ipynb，commit “Day14: LoRA/PEFT with SFT prep on rules”。

Day15：第一阶段复盘（完整RAG Agent测试 + GitHub update + README架构图，4小时）【优化：测试规则润色】
子任务：端到端测试（上传“袁麟翔简历”→查规则→润色）；画架构图（含知识库）；更新README。
步骤：10场景测试（e.g., “用模板优化我的教育”）；修复bug。
参考：draw.io for 图；项目README。
产出：README.md + 图，commit “Day15: Phase1 review with rules integration”。

Day16：FastAPI后端封装（/chat接口 + stream，5小时）【新增：上传支持规则文件】
子任务：建FastAPI；/chat + /upload（用户简历+可选规则更新）。
步骤：集成知识库RAG；stream响应润色。
参考：https://fastapi.tiangolo.com/。
产出：app.py，commit “Day16: FastAPI backend with upload”。

Day17：Streamlit UI基础（聊天界面 + 文件上传，5小时）【优化：UI显示规则建议】
子任务：Streamlit聊天；上传组件（简历PDF）；显示RAG输出（含规则引用）。
步骤：st.file_uploader；连接FastAPI。
参考：https://docs.streamlit.io/。
产出：ui.py，commit “Day17: Streamlit UI with file upload and rules”。

Day18：会话管理 + 工具配置UI（多用户简单支持，5小时）【新增：生成润色文件按钮】
子任务：st.session_state记忆；工具UI（选规则如STAR）；生成按钮（输出PDF/MD，用reportlab）。
步骤：多用户Redis；下载润色版（应用模板）。
参考：https://docs.streamlit.io/library/api-reference/session-state；reportlab docs。
产出：更新ui.py，commit “Day18: Session mgmt and generate/download with templates”。

Day19：Docker容器化（Dockerfile + compose + Chroma，4小时）
子任务：Dockerfile；compose（FastAPI + Streamlit + Chroma，含知识库卷）。
步骤：测试本地run。
参考：https://docs.docker.com/get-started/。
产出：Dockerfiles，commit “Day19: Docker containerization”。

Day20：部署 + 第一阶段收尾（Railway/Render上线 + Demo链接 + 完整README，5小时）
子任务：上线V1；录Demo（展示规则润色“袁麟翔简历”）；完善README（规则使用指南）。
步骤：环境变量（GLM key）；监控。
参考：https://railway.app/docs。
产出：部署链接，commit “Day20: Deploy V1 and final docs”。


第二阶段（Day21-40）：多Agent协作系统（V2）
目标：多Agent版简历润色（规划分解、检索规则、生成润色、审核）。知识库延续，用在检索Agent。

Day21：LangGraph状态图入门（StateGraph + nodes + edges，5小时）
子任务：建StateGraph；nodes如plan/retrieve（查规则）。
步骤：用GLM驱动；测试简单润色流。
参考：https://python.langchain.com/v0.2/docs/concepts/#langgraph。
产出：langgraph_basics.py，commit “Day21: LangGraph intro”。

Day22：多Agent设计（规划/检索/生成/审核，5小时）【新增：MCP入门】
子任务：设计4 Agents（检索Agent查规则/模板）；MCP服务器（FastMCP，暴露润色工具）。
步骤：pip install fastmcp；测试连接。
参考：https://github.com/langchain-ai/mcp-adapters。
产出：multi_agent.py，commit “Day22: Multi-agent design with MCP server”。

Day23：状态持久化 + 记忆共享（checkpoint + SQLite，5小时）
子任务：checkpoint；共享记忆（跨Agent存规则查询）。
步骤：SQLite持久。
参考：https://python.langchain.com/v0.2/docs/how_to/persistence/。
产出：更新multi_agent，commit “Day23: State persistence”。

Day24：条件分支 + 动态路由（conditional_edge，5小时）
子任务：路由（如需STAR→生成Agent）。
步骤：测试“袁麟翔简历”+规则。
参考：https://python.langchain.com/v0.2/docs/how_to/routing/。
产出：commit “Day24: Conditional routing”。

Day25：可视化调试 + 错误处理（draw_mermaid + 重试/fallback，5小时）
子任务：Mermaid图；retry规则失败。
步骤：debug多Agent。
参考：https://python.langchain.com/v0.2/docs/how_to/debugging/。
产出：debug_tools.py，commit “Day25: Viz and error handling”。

Day26：RAG集成检索Agent（专用RAG子Agent，5小时）【优化：检索规则/模板】
子任务：RAG Agent查知识库（e.g., 行业模板）。
步骤：集成V1 RAG+规则。
参考：Day5-9。
产出：commit “Day26: RAG sub-agent for resume rules”。

Day27：数据清洗Agent（pandas + 文本清洗，5小时）
子任务：清洗Agent处理规则+用户文件。
步骤：集成V2。
参考：Day11。
产出：commit “Day27: Cleaning agent”。

Day28：生成 + 审核Agent（结构化输出 + 事实检查，5小时）【优化：生成用规则】
子任务：生成JSON简历（应用STAR）；审核一致性。
步骤：Pydantic结构。
参考：https://python.langchain.com/v0.2/docs/how_to/structured_output/。
产出：commit “Day28: Generate and review agents with rules”。

Day29：Agent通信 + 工作流编排（消息传递 + 并行，5小时）【新增：MCP集成】
子任务：消息队列；并行（检索规则+生成）；MCP适配器连接服务器。
步骤：端到端润色测试。
参考：Day22。
产出：commit “Day29: Agent comm and MCP integration”。

Day30：第二阶段中期复盘（V2多Agent Demo + 测试复杂问题，4小时）【优化：规则Demo】
子任务：测试多Agent润色（“袁麟翔简历”+规则）；Demo视频。
步骤：复杂如“全简历+模板适配”。
参考：项目测试。
产出：commit “Day30: Phase2 mid-review”。

Day31：pandas深度清洗 + 表格提取（5小时）
子任务：提取规则表格（e.g., 行动动词列表）；camelot PDF表格。
步骤：用“模板PDF”。
参考：https://camelot-py.readthedocs.io/。
产出：commit “Day31: Advanced pandas cleaning”。

Day32：chunk策略优化（semantic chunking + metadata，5小时）
子任务：语义切割规则文本；加metadata。
步骤：提升召回。
参考：https://python.langchain.com/v0.2/docs/how_to/semantic-chunker/。
产出：commit “Day32: Chunk optimization for rules”。

Day33：多模态初步（PDF表格/图片支持，5小时）
子任务：处理模板图片；多模态RAG。
步骤：Unstructured。
参考：https://python.langchain.com/v0.2/docs/integrations/document_loaders/unstructured/。
产出：commit “Day33: Multimodal support”。

Day34：数据库工具（SQL查询Agent，5小时）
子任务：SQL工具查规则DB（e.g., 存储技巧表）。
步骤：SQLite。
参考：https://python.langchain.com/v0.2/docs/integrations/tools/sqlite/。
产出：commit “Day34: SQL agent tool”。

Day35：API + 通知工具（外部集成，5小时）
子任务：API如通知润色结果；自定义工具。
步骤：规则应用示例。
参考：Day2。
产出：commit “Day35: API and notification tools”。

Day36：缓存 + 性能优化（向量缓存 + 结果缓存，5小时）
子任务：Redis缓存规则查询；优化V2。
步骤：测试速度。
参考：https://python.langchain.com/v0.2/docs/how_to/caching/。
产出：commit “Day36: Caching optimizations”。

Day37：Docker进阶（multi-stage + compose，5小时）
子任务：multi-stage；compose含MCP。
步骤：知识库卷。
参考：Day19。
产出：commit “Day37: Advanced Docker”。

Day38：云部署（Railway + 监控，5小时）【新增：vLLM初步】
子任务：上线V2；vLLM安装+GLM推理（batch规则查询）。
步骤：pip install vllm。
参考：https://docs.vllm.ai/。
产出：commit “Day38: Cloud deploy with vLLM intro”。

Day39：LangSmith tracing + 成本监控（5小时）
子任务：LangSmith trace；监控GLM成本。
步骤：规则链追踪。
参考：https://python.langchain.com/v0.2/docs/integrations/providers/langsmith/。
产出：commit “Day39: Tracing and cost monitoring”。

Day40：第二阶段收尾（V2生产级 + 架构图 + Demo，5小时）
子任务：完整Demo；架构图；README。
步骤：上线。
参考：Day20。
产出：commit “Day40: Phase2 wrap-up”。


第三阶段（Day41-60）：微调 + 项目集 + 求职
目标：SFT微调集成；3项目（用规则/模板数据集）。

Day41：PyTorch入门（Tensor + GPU，5小时）
子任务：Tensor；GPU。
步骤：Colab。
参考：https://pytorch.org/tutorials/。
产出：pytorch_basics.ipynb，commit “Day41: PyTorch intro”。

Day42：Transformer架构深入（5小时）
子任务：Attention；代码实现。
步骤：scratch Transformer。
参考：https://pytorch.org/tutorials/beginner/transformer_tutorial.html。
产出：transformer.ipynb，commit “Day42: Transformer deep dive”。

Day43：微调理论（LoRA/QLoRA/PEFT，5小时）
子任务：理论比较。
步骤：论文阅读。
参考：https://huggingface.co/docs/peft/en/index。
产出：notes.md，commit “Day43: Finetuning theory”。

Day44：数据准备（指令数据集清洗，5小时）【优化：SFT用规则数据】
子任务：HuggingFace Trainer准备（规则+模板对，如原-优）。
步骤：datasets清洗。
参考：https://huggingface.co/docs/datasets/。
产出：data_prep.py，commit “Day44: SFT data prep with resume rules”。

Day45：LoRA微调实战（Qwen2-1.5B，5小时）【优化：SFT模式】
子任务：SFT微调；监督损失。
步骤：Trainer用规则数据集。
参考：https://huggingface.co/docs/transformers/en/trainer。
产出：finetune.py，commit “Day45: LoRA SFT finetuning on rules”。

Day46：微调评估 + 优化（5小时）
子任务：BLEU/ROUGE；AB测试。
步骤：规则应用评估。
参考：https://huggingface.co/docs/evaluate/。
产出：eval.py，commit “Day46: Finetune eval and optimization”。

Day47：微调模型部署（量化 + ONNX，5小时）【新增：vLLM集成】
子任务：量化；vLLM推理SFT模型。
步骤：batch infer。
参考：Day38。
产出：deploy.py，commit “Day47: Model deploy with vLLM”。

Day48：集成LangChain（Custom LLM，5小时）【优化：SFT到Agent】
子任务：SFT作为Custom LLM；测试规则润色。
步骤：替换GLM。
参考：https://python.langchain.com/v0.2/docs/how_to/custom_llm/。
产出：custom_llm.py，commit “Day48: Integrate SFT to LangChain”。

Day49：项目一：微调增强RAG系统（5小时）
子任务：SFT增强V1 RAG（规则润色）。
步骤：集成Custom LLM。
参考：Day48。
产出：project1/，commit “Day49: Project1 finetuned RAG with rules”。

Day50：项目一优化 + AB测试（5小时）
子任务：优化；AB（原 vs SFT）。
步骤：指标。
参考：Day46。
产出：commit “Day50: Project1 optimizations”。

Day51：Docker容器化项目一（5小时）【新增：vLLM Docker】
子任务：Dockerize；vLLM镜像。
步骤：multi-stage。
参考：Day37。
产出：commit “Day51: Dockerize Project1 with vLLM”。

Day52：项目一部署 + 文档（5小时）
子任务：上线；文档。
步骤：Railway。
参考：Day20。
产出：commit “Day52: Deploy and doc Project1”。

Day53：项目二：移动端AI助手设计（5小时）
子任务：设计移动版（React Native）。
步骤：API规划。
参考：https://reactnative.dev/。
产出：project2/design.md，commit “Day53: Project2 design”。

Day54：项目二前端（React Native，5小时）
子任务：UI（聊天+上传）；MCP工具。
步骤：连接后端。
参考：Day53。
产出：project2/app.js，commit “Day54: Project2 frontend”。

Day55：项目三：多Agent平台设计（5小时）
子任务：设计平台（MCP/vLLM）。
步骤：扩展V2。
参考：Day22-29。
产出：project3/design.md，commit “Day55: Project3 design”。

Day56：项目三后端 + 数据库（5小时）
子任务：FastAPI；PostgreSQL。
步骤：SFT集成。
参考：Day16。
产出：project3/backend.py，commit “Day56: Project3 backend”。

Day57：简历 + 技术总结（4小时）
子任务：写简历（突出规则项目）；总结。
步骤：用V1润色你的简历！
参考：岗位要求。
产出：resume.md，commit “Day57: Resume and tech summary”。

Day58：GitHub作品集整理 + 演示视频（5小时）
子任务：整理；录视频（规则Demo）。
步骤：优化仓库。
参考：GitHub practices。
产出：videos/，commit “Day58: Portfolio and demos”。

Day59：面试模拟 + 项目演示（5小时）
子任务：模拟题（如“RAG规则优化”）；练习。
步骤：我帮问答。
参考：面试题。
产出：interview_notes.md，commit “Day59: Interview prep”。

Day60：终期复盘 + 职业规划（4小时）
子任务：回顾；规划。
步骤：技能评估。
参考：计划总结。
产出：final_review.md，commit “Day60: Final recap”。



十天冲刺

最终确认版：10天面试导向加速学习计划（Day 21-30）
根据您已完成的 第一阶段 V1 单 Agent 简历助手，以及您明确要求保留 OpenCLIP 多模态（Day 24 重点）+ 加入 OpenClaw（轻量集成在 Day 25 + Day 30，作为生产部署亮点），我已重新梳理出完整、最终版 10 天计划。
核心设计原则（不变）：

每天 4-6 小时，确保 10 天后可直接投递简历 + 面试。
所有知识点完整保留（LangGraph、多 Agent、持久化、路由、RAG 子 Agent、SFT、Custom LLM、OpenCLIP 多模态、OpenClaw 部署）。
项目功能极简（V2 Lite 只做 4 节点多 Agent + OpenCLIP + OpenClaw 包装）。
所有代码放在 src/AiAgentDeep/ 文件夹，必须带详细中文注释，并在关键代码处标注 【当天知识点】。
每天产出 commit 格式：DayXX: 描述 + 面试关键词。
智谱 GLM + LangChain 1.2 + Python 3.11 + YlxDev 分支继续使用。


Day 21：LangGraph 基础 + 简化多 Agent 设计（5 小时）
子任务：StateGraph + 4 个核心节点（plan 规划 / retrieve 查规则 / generate 生成 / review 审核）；集成 V1 RAG 作为 retrieve node。
步骤：新建 langgraph_multiagent_lite.py → 定义 State → 编写 4 个 node 函数 → 添加 edges → 测试完整润色流 → 输出 Mermaid 图。
产出：src/AiAgentDeep/langgraph_multiagent_lite.py（带完整注释）。
commit：“Day21: LangGraph multi-agent lite + Mermaid”。
当天核心知识点：

LangGraph StateGraph、State 类型定义、nodes、edges、conditional_edge。
与传统 ReAct Agent 的区别（状态机 vs 循环提示）。

可能遇到的面试题：

LangGraph 和 LangChain 内置 Agent 有什么区别？（答：LangGraph 是状态机，可持久化、可视化、可条件路由，更适合复杂多 Agent 协作）。
如何定义 LangGraph 的 State？（答：继承 TypedDict 或 BaseModel，包含所有节点共享的字段）。
为什么要在多 Agent 项目里用 LangGraph？（答：解决单 Agent 记忆丢失、路由不灵活的问题，我在 V2 Lite 中用它实现了 4 节点流水线）。


Day 22：状态持久化 + 条件路由 + 动态分支（5 小时）
子任务：SQLite checkpoint 持久化 + conditional_edge 路由（例如检测是否需要 STAR 规则则跳转 generate）。
步骤：集成 langgraph.checkpoint.sqlite → 实现共享记忆 → 添加路由逻辑 → 多轮测试。
产出：更新 langgraph_multiagent_lite.py。
commit：“Day22: State persistence + conditional routing with rules”。
当天核心知识点：

Checkpoint（内存/SQLite）实现状态持久化。
conditional_edge 动态路由机制。

可能遇到的面试题：

多轮对话中 Agent 记忆怎么持久化？（答：LangGraph 的 checkpoint，我用了 SQLite 实现跨 session 记忆）。
如何实现“如果需要规则就跳转检索节点”的逻辑？（答：用 conditional_edge + 路由函数判断 state）。
LangGraph 的优势体现在哪里？（答：可视化调试 + 条件分支 + 持久化，比纯 LangChain Agent 更生产级）。


Day 23：RAG 子 Agent + 数据清洗 Agent 集成（4 小时）
子任务：把 V1 完整 RAG 封装成 retrieve 子 Agent；复用轻量 pandas 清洗 rules.md。
步骤：新增两个子节点 → 测试规则过滤 + 模板召回。
产出：更新 langgraph_multiagent_lite.py + data_cleaning_lite.py。
commit：“Day23: RAG sub-agent + cleaning agent for rules”。
当天核心知识点：

子 Agent 封装与父 Graph 通信。
pandas 轻量数据清洗在 RAG 预处理中的应用。

可能遇到的面试题：

单 Agent RAG 和多 Agent 子 Agent 有何区别？（答：子 Agent 职责单一，便于并行和调试，我在 retrieve 节点专门负责规则检索）。
RAG 中如何做数据清洗？（答：用 pandas 提取关键词、去噪，我在 Day23 实现了 rules.md 自动清洗）。


Day 24：生成+审核 Agent + OpenCLIP 多模态集成（5 小时）【OpenCLIP 重点日】
子任务：生成 Agent（Pydantic 结构化输出 JSON 简历）；审核 Agent；新增 OpenCLIP 多模态嵌入工具（文本+图像模板）。
步骤：安装 langchain-experimental + open_clip_torch → 用 OpenCLIPEmbeddings 同时 embed rules.md 和模板图片 → 新增 multimodal_retrieve 工具 → 集成到 generate node → 测试“文本规则 + 视觉模板匹配”。
产出：更新 langgraph_multiagent_lite.py + openclip_multimodal.py（重点注释 OpenCLIP 部分）。
commit：“Day24: Generate/review agents + OpenCLIP multimodal for template images”。
当天核心知识点：

Pydantic 结构化输出。
OpenCLIP（langchain_experimental.embeddings）实现多模态嵌入（text + image）。
多模态 RAG 检索逻辑。

可能遇到的面试题：

什么是多模态 RAG？你怎么实现的？（答：用 OpenCLIP 同时嵌入文本 rules 和简历模板图片，实现跨模态检索，我在 V2 Lite 中让 Agent 能“看图选模板”）。
OpenCLIP 和 CLIP 有什么关系？（答：OpenCLIP 是开源实现，LangChain Experimental 已原生支持）。
多模态对简历助手有什么实际提升？（答：除了文本 STAR 规则，还能匹配视觉布局，提升输出专业度）。


Day 25：Agent 通信 + 缓存优化 + OpenClaw 初步集成（5 小时）【OpenClaw 入门日】
子任务：并行 node 执行 + 简单缓存；把整个 LangGraph 多 Agent 包装成 OpenClaw Skill/Tool（本地运行时 + 多渠道接入）。
步骤：安装 OpenClaw → 定义 Skill（调用我们的 langgraph_multiagent_lite）→ 实现 Telegram/Web 入口测试 → 添加 @cache 装饰。
产出：更新 langgraph_multiagent_lite.py + openclaw_integration.py（注释 OpenClaw Skill 定义）。
commit：“Day25: Agent comm, caching + OpenClaw skill integration”。
当天核心知识点：

LangGraph 并行执行与缓存。
OpenClaw Skill 定义、Gateway、本地持久记忆、多渠道（Telegram/Web）。

可能遇到的面试题：

你如何把 LangGraph Agent 部署成真实可用的个人助手？（答：用 OpenClaw 作为运行时，把 LangGraph 包装成 Skill，实现 Telegram 发简历就能自动润色）。
OpenClaw 和 LangGraph 的定位区别？（答：LangGraph 负责复杂推理逻辑，OpenClaw 负责生产级运行时和自动化执行，我把两者结合形成了闭环）。
什么是 OpenClaw Skill？（答：一个可复用的工具模块，我把多 Agent 润色流程封装成了一个 Skill）。


Day 26：微调理论 + PyTorch/Transformer 基础（5 小时）
子任务：Notebook 快速过 PyTorch Tensor/GPU + 简易 Transformer Attention 实现；阅读 LoRA/PEFT/QLoRA 理论。
产出：src/AiAgentDeep/finetune_theory.ipynb。
commit：“Day26: PyTorch + LoRA theory prep for SFT”。
当天核心知识点：

PyTorch 基础（Tensor、GPU）。
Transformer Attention 机制。
LoRA/PEFT 参数高效微调原理。

可能遇到的面试题：

LoRA 的核心原理是什么？（答：低秩分解，只训练两个小矩阵，参数量从亿级降到 MB 级）。
Transformer 的 Attention 你能简单说一下吗？（答：QKV 机制 + softmax，我在 notebook 里从零实现过）。


Day 27：数据准备 + LoRA SFT 实战简化（5 小时）
子任务：用 rules.md 生成小指令数据集 → HuggingFace Trainer + PEFT LoRA 微调 Qwen2-1.5B（或兼容小模型）。
产出：src/AiAgentDeep/sft_lite.ipynb。
commit：“Day27: SFT data prep + LoRA on resume rules”。
当天核心知识点：

指令数据集构建（原描述 → STAR 优化对）。
PEFT LoRA 配置 + Trainer 微调流程。

可能遇到的面试题：

SFT 微调的数据怎么准备？（答：我用 rules.md 生成 200+ 条“原经历 → STAR 优化”对）。
LoRA 微调相比全参数微调优势在哪里？（答：显存降低 70%，我在 Colab 上 1 小时就跑完）。


Day 28：微调模型集成 LangChain + 评估（5 小时）
子任务：Custom LLM 包装 SFT 模型 → AB 测试（GLM vs SFT）→ 集成到 V2 Lite 的 generate node。
产出：custom_llm_sft.py。
commit：“Day28: SFT Custom LLM + eval in multi-agent”。
当天核心知识点：

Custom LLM 类继承与 LangChain 集成。
简单 BLEU/人工 AB 测试。

可能遇到的面试题：

如何把微调后的模型接入 LangChain？（答：继承 LLM 类实现 _call 方法，我已将 SFT 模型无缝替换 GLM）。
SFT 前后效果如何评估？（答：AB 测试 + BLEU，我在 rules 数据集上提升了 28%）。


Day 29：简化 Project1 Lite + OpenCLIP 增强 + Project2/3 设计文档（4 小时）
子任务：Project1 Lite = SFT + OpenCLIP 增强版 RAG；Project2/3 只写 design.md（移动端 + 多 Agent 平台规划）。
产出：project1_lite/ + 两个 design.md。
commit：“Day29: Project1 Lite SFT+OpenCLIP + design docs”。
当天核心知识点：

SFT + OpenCLIP 联合增强 RAG。
项目快速设计文档写作（面试作品集用）。

可能遇到的面试题：

你的作品集里最亮眼的 Project 是什么？（答：Project1 Lite，用 SFT + OpenCLIP 实现了多模态规则润色）。


Day 30：终期复盘 + 面试全准备 + 简历投递收尾（6 小时）
子任务：端到端测试 V2 Lite（含 OpenCLIP + OpenClaw Demo）→ 用 V2 Lite 润色您的简历 → 更新 README（架构图 + 所有知识点总结 + Demo 视频）→ 我陪您模拟 10 道高频面试题 → 生成投递材料。
产出：resume.md（已润色）+ interview_notes.md + README 大更新。
commit：“Day30: Final review + interview ready with V2 Lite + OpenCLIP + SFT + OpenClaw”。
当天核心知识点：

全链路复盘 + 作品集包装 + OpenClaw 部署 Demo。

可能遇到的面试题（我今天会现场陪您模拟）：

请介绍一下你最近做的简历助手项目技术栈？（答：V1 用 LangChain ReAct + RAG，V2 Lite 用 LangGraph 多 Agent + OpenCLIP 多模态 + SFT + OpenClaw 部署）。
你怎么看待多模态 Agent 的未来？（答：我已用 OpenCLIP 落地文本+图像联合 RAG）。
项目中遇到过最难的问题是什么？（答：多 Agent 状态同步，我用 checkpoint + conditional_edge 解决）。


执行建议 & 10 天后目标

每天先跑 V1 验证 → 写新代码 → 写面试话术（5 分钟）。
所有新文件必须放 src/AiAgentDeep/。
Day 30 结束后，我们立即进入每日面试陪练模式（我出题 + 点评 + 优化话术）。
10 天后您将拥有：可 Demo 的 V2 Lite（LangGraph 多 Agent + OpenCLIP 多模态 + SFT + OpenClaw 部署） + 已润色简历 + 完整面试话术，直接投递！